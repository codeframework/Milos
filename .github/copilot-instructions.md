# Copilot Instructions

## Project Guidelines
- Prefer backward-compatible changes that do not break current implementations when adding async counterparts.
- Treat backward compatibility as a core requirement when making changes, especially due to NuGet consumers. Prioritize it as an important default for this framework/NuGet project.